@language("no-prelude")

# Read a `Bytes` from a file.
#
# Example:
#
#     bytes: readFile("sqlite.db")
#
# @arg filePath The path of the file to read. Relative paths are
#               considered relative to the directory where the
#               compiler is invoked, not the location of the
#               composition.
#
# @return The contents of the file.
fun readFile(filePath: String) -> Bytes: __Ludwig_Bytes_readFile(filePath)
